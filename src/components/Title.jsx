const Title = (prop) => {
  return (
    <div className="title text-[50px] border-2 border-solid border-[rgba(0,0,0,0.35)] font-[600] text-center">
      {prop.title}
    </div>
  );
};

export default Title;
